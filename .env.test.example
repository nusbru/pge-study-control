# Optional when running dotnet directly against your own disposable test database.
ConnectionStrings__Database=Host=127.0.0.1;Port=5433;Database=pge_identity_test;Username=pge;Password=test_only
# npm run test:e2e and dotnet test provision isolated databases automatically.
